<div id="profile">

  <%= render partial: "profile.current.user", locals: {
                                                user:              current_user,
                                                authorizations:    current_user.authorizations,
                                                active_services:   current_user.services,
                                                inactive_services: current_user.inactive_services
                                            } %>

  <%= render partial: "profile.groups", locals: { groups: @owned_groups } %>

</div>
