<div class="row">
  <div class="col s8">
    <%= form_for group do |f| %>
      <div class="row">

        <div class="input-field col s12">
          <%= f.text_field :name %>
          <%= f.label :name %>
        </div>

        <div class="input-field col s12">
          <%= f.text_field :description %>
          <%= f.label :description %>
        </div>

        <%= f.label :users %>
        <%= f.collection_check_boxes :user_ids, users, :id, :username do |b| %>
          <p>
            <%= b.check_box %>
            <%= b.label %>
          </p>
        <% end %>

        <%= f.hidden_field :owner_id, value: owner_id %>

        <%= f.submit "Create Group", class: "btn" %>
      </div>
    <% end %>
  </div>
</div>
